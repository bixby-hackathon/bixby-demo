action (FindPokemon) {
  type (Search)
  description (Demonstrate a simple pokemon GET with the http library.)
  collect {
    input (pokemonName) {
      type (PokemonSpecies)
      min (Required) max (One)
    }
  }
  output (Pokemon)
}